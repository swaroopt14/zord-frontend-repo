apiVersion: v1
kind: Service
metadata:
  name: zord-token-enclave-postgres
  namespace: arealis-zord
spec:
  selector:
    app: zord-token-enclave-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zord-token-enclave-postgres
  namespace: arealis-zord
spec:
  serviceName: zord-token-enclave-postgres
  replicas: 1
  selector:
    matchLabels:
      app: zord-token-enclave-postgres
  template:
    metadata:
      labels:
        app: zord-token-enclave-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: token_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: zord-secrets
                  key: TOKEN_DB_PASSWORD
            - name: POSTGRES_DB
              value: zord_token_enclave_db
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U token_user -d zord_token_enclave_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
