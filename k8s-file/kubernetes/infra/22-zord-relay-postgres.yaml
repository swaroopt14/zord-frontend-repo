apiVersion: v1
kind: Service
metadata:
  name: zord-relay-postgres
  namespace: arealis-zord
spec:
  selector:
    app: zord-relay-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zord-relay-postgres
  namespace: arealis-zord
spec:
  serviceName: zord-relay-postgres
  replicas: 1
  selector:
    matchLabels:
      app: zord-relay-postgres
  template:
    metadata:
      labels:
        app: zord-relay-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: relay_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: zord-secrets
                  key: RELAY_DB_PASSWORD
            - name: POSTGRES_DB
              value: zord_relay_db
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U relay_user -d zord_relay_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
