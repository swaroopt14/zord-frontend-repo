apiVersion: v1
kind: Service
metadata:
  name: zord-intent-postgres
  namespace: arealis-zord
spec:
  selector:
    app: zord-intent-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zord-intent-postgres
  namespace: arealis-zord
spec:
  serviceName: zord-intent-postgres
  replicas: 1
  selector:
    matchLabels:
      app: zord-intent-postgres
  template:
    metadata:
      labels:
        app: zord-intent-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: intent_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: zord-secrets
                  key: INTENT_DB_PASSWORD
            - name: POSTGRES_DB
              value: zord_intent_engine_db
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U intent_user -d zord_intent_engine_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
